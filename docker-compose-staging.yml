version: "2"
services:
  app:
    container_name: easy-homeworks-back-staging
    build:
      context: ./
      dockerfile: ./Dockerfile.prod
    ports:
      - 8080:8081
    volumes:
      - ./src:/app/src
